# local config (may contain machine-specific paths / overrides)
config.yaml

# portal job uploads (contain private documents)
.jobs/

# reports
rename_report.csv
*.report.csv

# python
__pycache__/
*.pyc
.venv/
venv/

# scans / private documents - never commit real HR files
samples/*.pdf
!samples/.gitkeep
